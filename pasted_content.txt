# Prompt: Improve AQI Dashboard with AQI-Based Recommendations

I am developing a BE minor project called **Air Quality Detection and Anomaly Detection System** using Django.

The project already has:

* Air quality data collected through an API
* SQLite database
* Data preprocessing
* EDA
* AQI prediction using a trained Random Forest model
* Anomaly detection using Isolation Forest
* Django web application
* Existing logo and website styling

I want you to improve the Django dashboard so that it provides meaningful **AQI-based information, health status, colors, recommendations, and suggestions**.

## IMPORTANT

Before making changes:

1. Inspect the existing Django project.
2. Inspect the existing database structure.
3. Inspect how AQI is currently calculated/stored.
4. Inspect the existing ML prediction code.
5. Inspect the existing anomaly detection code.
6. Inspect the existing HTML/CSS/JS.
7. Do NOT modify the trained ML models.
8. Do NOT modify the data collection system.
9. Do NOT change the database schema unless absolutely necessary.
10. Reuse the existing architecture instead of creating duplicate functionality.

---

## 1. AQI Classification

The project uses the OpenWeather-style AQI scale from **1 to 5**.

Implement a centralized AQI classification function/service.

Use:

| AQI | Category  | Meaning                       |
| --- | --------- | ----------------------------- |
| 1   | Good      | Air quality is good           |
| 2   | Fair      | Air quality is acceptable     |
| 3   | Moderate  | Some pollution is present     |
| 4   | Poor      | Air quality is unhealthy      |
| 5   | Very Poor | Air quality is very unhealthy |

Do not scatter these conditions throughout multiple HTML files.

Create one reusable Django service/helper for AQI classification.

The function should return:

* AQI value
* Category
* Status
* CSS class/color identifier
* Health recommendation
* General suggestion

---

## 2. Dynamic AQI Colors

Make the AQI card dynamically change according to the AQI category.

Use an intuitive color system:

* AQI 1 → Green
* AQI 2 → Yellow/Light Green
* AQI 3 → Orange
* AQI 4 → Red
* AQI 5 → Dark Red

Do not hard-code the color separately in every template.

Use CSS classes such as:

```text
aqi-good
aqi-fair
aqi-moderate
aqi-poor
aqi-very-poor
```

The Django backend should determine the appropriate class.

---

## 3. Current AQI Card

Improve the current AQI card.

It should clearly display:

```text
CURRENT AQI

3

MODERATE

Some pollution is present.
```

The card should include:

* Large AQI number
* AQI category
* Short explanation
* Dynamic color
* Appropriate visual icon if suitable

Keep the design clean and professional.

---

## 4. Health Recommendations

Create AQI-based health recommendations.

For example:

### AQI 1 — Good

```text
Air quality is good.

Recommendation:
Air quality is suitable for normal outdoor activities.
```

### AQI 2 — Fair

```text
Air quality is acceptable.

Recommendation:
Most people can continue normal outdoor activities.
Sensitive individuals should monitor their symptoms.
```

### AQI 3 — Moderate

```text
Air quality is moderately polluted.

Recommendation:
Sensitive individuals should consider reducing prolonged outdoor activities.
```

### AQI 4 — Poor

```text
Air quality is poor.

Recommendation:
Consider reducing prolonged outdoor activities, especially for sensitive individuals.
```

### AQI 5 — Very Poor

```text
Air quality is very poor.

Recommendation:
Avoid or reduce outdoor activities, particularly for sensitive individuals.
```

Keep recommendations general and educational. Do not present them as medical diagnosis.

---

## 5. Pollutant Information

For the dashboard, display:

* PM2.5
* PM10
* CO
* NO2
* O3
* SO2

Each pollutant should have:

* Current value
* Unit
* Simple description

For example:

```text
PM2.5
18.4 μg/m³

Fine particulate matter
```

Make the pollutant cards visually consistent.

---

## 6. Predicted AQI

Create a separate card:

```text
PREDICTED AQI

4

POOR
```

Compare the current AQI and predicted AQI.

For example:

```text
Current AQI: 3
Predicted AQI: 4

⚠ Air quality may worsen.
```

or:

```text
Current AQI: 4
Predicted AQI: 3

✓ Air quality may improve.
```

If both are the same:

```text
Air quality is expected to remain similar.
```

Create this comparison dynamically.

Do not invent predictions. Use the actual Random Forest model output.

---

## 7. Anomaly Detection Status

Create a dedicated anomaly status section.

If Isolation Forest returns `1`:

```text
NORMAL READING

No unusual pollution pattern detected.
```

If Isolation Forest returns `-1`:

```text
⚠ ANOMALY DETECTED

This reading shows an unusual pollution pattern.
```

Make the anomaly section visually noticeable but not excessively alarming.

Use a warning color for anomalies.

---

## 8. Smart Suggestions

Add a section called:

```text
AIR QUALITY SUGGESTIONS
```

Suggestions should change based on AQI.

Examples:

### AQI 1

* Outdoor activities are generally suitable.
* Normal daily activities can continue.

### AQI 2

* Normal outdoor activities are generally acceptable.
* Sensitive individuals may monitor air quality.

### AQI 3

* Consider limiting prolonged outdoor activities.
* Sensitive individuals should take extra care.
* Consider checking air quality before outdoor exercise.

### AQI 4

* Reduce prolonged outdoor exposure.
* Consider indoor activities when possible.
* Sensitive individuals should take extra precautions.

### AQI 5

* Avoid prolonged outdoor exposure.
* Consider staying indoors when possible.
* Monitor air quality updates regularly.

Keep these suggestions concise.

---

## 9. AQI Trend

Improve the historical AQI chart.

Display:

```text
AQI TREND

Previous readings ─────────── Current
```

Use the actual timestamp and AQI values from the database.

Make the chart responsive.

If possible, visually indicate higher AQI values.

Do not create fake data.

---

## 10. Prediction Trend

If the existing project has enough historical prediction data, display a prediction-related visualization.

If sufficient data is not available, do not create fake historical predictions.

Instead display only the latest:

```text
Current AQI
Predicted AQI
Difference
```

---

## 11. Last Updated Information

Clearly show:

```text
Last updated:
18 Aug 2026, 01:00 PM
```

Use the actual timestamp from the database.

Also show:

```text
Data source: Air Quality API
```

Do not display a fake timestamp.

---

## 12. Dashboard Layout

Improve the dashboard layout to something like:

```text
--------------------------------------------------
              AIR QUALITY DASHBOARD
--------------------------------------------------

 Current AQI          Predicted AQI
      3                     4
   MODERATE                POOR

--------------------------------------------------

 Air Quality Status
 Some pollution is present.

 Health Recommendation
 Consider reducing prolonged outdoor activities.

--------------------------------------------------

 PM2.5     PM10       CO       NO2       O3       SO2

--------------------------------------------------

                AQI TREND

--------------------------------------------------

             ANOMALY STATUS

        ✓ Normal Reading

--------------------------------------------------

             AIR QUALITY
             SUGGESTIONS

 • ...
 • ...
 • ...

--------------------------------------------------

 Last Updated: ...
```

Make the dashboard responsive for:

* Desktop
* Laptop
* Tablet
* Mobile

---

## 13. Navigation

Keep or create a simple navigation bar:

```text
Logo

Dashboard
Prediction
Historical Data
Anomalies
About
```

Use the existing project logo.

Do not redesign the logo.

Keep the navigation clean and professional.

---

## 14. Color Theme

Use a professional environmental/air-quality theme.

Suggested palette:

```text
Primary:
#1B5E20

Secondary:
#2E7D32

Background:
#F5F9F6

Card:
#FFFFFF

Text:
#263238

AQI Good:
#2E7D32

AQI Fair:
#9E9D24

AQI Moderate:
#EF6C00

AQI Poor:
#C62828

AQI Very Poor:
#8E0000
```

Use these colors consistently.

Avoid making the entire website extremely colorful.

The AQI color should mainly be used to communicate air-quality status.

---

## 15. Backend Architecture

Keep AQI logic in Django services rather than placing business logic directly inside templates.

For example:

```text
dashboard/
├── views.py
├── services.py
├── urls.py
└── ...
```

Use reusable functions such as:

```python
get_aqi_category(aqi)
get_health_recommendation(aqi)
get_aqi_color_class(aqi)
get_aqi_suggestions(aqi)
compare_aqi(current_aqi, predicted_aqi)
```

Do not duplicate these conditions in multiple views.

---

## 16. Error Handling

If the database has no data, show:

```text
No air quality data available.
```

If the ML prediction cannot be generated:

```text
Prediction currently unavailable.
```

If anomaly detection cannot be performed:

```text
Anomaly detection currently unavailable.
```

Do not crash the entire dashboard because of one missing component.

Do not hide actual errors during development.

---

## 17. Important ML Requirement

Do not change the existing ML pipeline.

Before connecting the Random Forest and Isolation Forest:

* Inspect their training code.
* Determine the exact feature names.
* Determine the exact feature order.
* Use the same preprocessing assumptions.
* Use the existing `.pkl` files.
* Do not retrain the models.
* Do not invent missing features.

If there is a mismatch between the Django data and the model's expected features, explain the mismatch before changing anything.

---

## 18. Final Requirement

After implementation:

1. Run Django checks.
2. Run the application.
3. Test the dashboard.
4. Test AQI 1.
5. Test AQI 3.
6. Test AQI 5.
7. Test predicted AQI.
8. Test anomaly detection.
9. Test when database data is unavailable.
10. Check responsive layout.
11. Fix any errors introduced by your changes.

Before making major changes, explain what you plan to modify.

Do not rewrite working parts of the project unnecessarily.

The final result should look like a **professional BE minor project**, not an overcomplicated commercial application.
